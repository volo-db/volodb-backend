application:
  formatted-version: @project.version@
  title: @project.name@

volodb:
  minio:
    url: http://minio:${MINIO_PORT}
    access-key: ${MINIO_ACCESS_KEY}
    secret-key: ${MINIO_SECRET_KEY}

spring:
  application:
    name: "VoloDB"
  banner:
    location: "banner.txt"
  main:
    banner-mode: "log"
  datasource:
    url: "jdbc:mysql://mysql:${MYSQL_PORT}/volo_db"
    username: root
    password: ${MYSQL_ROOT_PASSWORD}
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1

  jpa:
    hibernate:
      naming-strategy: "org.hibernate.cfg.EJB3NamingStrategy"

server:
  servlet:
    contextPath: "/api/v1"

springdoc:
  api-docs:
    path: "/docs"

  swagger-ui:
    path: "/docs/swagger"

security:
  jwt:
    secret-key: ${JWT_SECRETKEY}
    token-duration: 12H

logging:
  level:
    org:
      springframework:
        security: DEBUG
        web:
          cors: DEBUG